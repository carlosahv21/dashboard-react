import React from "react";

const Roles = () => {
    return (
        <div>
            <h2>Roles</h2>
            <p>Manage user roles and permissions here.</p>
        </div>
    );
};

export default Roles;
