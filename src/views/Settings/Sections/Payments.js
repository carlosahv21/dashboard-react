import React from "react";

const Payments = () => {
    return (
        <div>
            <h2>Payments</h2>
            <p>Payment settings (future implementation).</p>
        </div>
    );
};

export default Payments;
